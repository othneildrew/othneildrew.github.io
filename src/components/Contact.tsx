import {SectionHeader} from '@/components/SectionHeader';

export const Contact = () => {
  const a = '';

  return (
    <section id="contact" className="container mx-auto mt-[370px] px-4">
      <SectionHeader subhead="Let's talk about your next project or role!">Interested in working together?</SectionHeader>

      <button>
        Connect on linkedIn
      </button>
    </section>
  );
}