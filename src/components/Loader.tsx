'use client';

export const Loader = () => {
  const a = '';

  return (
    <div className="relative h-screen flex flex-col justify-center border-4">
      <p className="text-white/70">is loadasing</p>

    </div>
  );
}